OUTPUT_DIR: "model/20201205_X_dota"
INPUT:
  MIN_SIZE_TRAIN: (1024,)
  MAX_SIZE_TRAIN: 1024
  MIN_SIZE_TEST:  1024
  MAX_SIZE_TEST:  1024
  RANDOM_ROTATE_ON: True # (0, 90, 180, or 270)
  BRIGHTNESS: 0.
  CONTRAST: 0.
  SATURATION: 0.
  HUE: 0.
MODEL:
  META_ARCHITECTURE: "GeneralizedRCNN"
  WEIGHT: "catalog://ImageNetPretrained/FAIR/20171220/X-101-32x8d"
  RPN_ONLY: True
  FCOS_ON: True
  BACKBONE:
    CONV_BODY: "R-101-FPN-RETINANET"
  RESNETS:
    STRIDE_IN_1X1: False
    BACKBONE_OUT_CHANNELS: 256
    NUM_GROUPS: 32
    WIDTH_PER_GROUP: 8
  RETINANET:
    USE_C5: False # FCOS uses P5 instead of C5
DATASETS:
  TRAIN: ("dota_trainval_cut", "dota_test_cut")
  TEST: ("dota_test_cut",)
DATALOADER:
  SIZE_DIVISIBILITY: 32
SOLVER:
  BASE_LR: 0.01
  WEIGHT_DECAY: 0.0001
  STEPS: (38000, 46000)
  MAX_ITER: 50000
  IMS_PER_BATCH: 12  #原设置12
  WARMUP_METHOD: "constant"
TEST:
  IMS_PER_BATCH: 12
#TEST:
#  BBOX_AUG:
#    ENABLED: False
#    H_FLIP: True
#    SCALES: (400, 500, 600, 700, 900, 1000, 1100, 1200)
#    MAX_SIZE: 2000
#    SCALE_H_FLIP: True
